<%= form_for(user_mention) do |f| %>
  <% if user_mention.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(user_mention.errors.count, "error") %> prohibited this user_mention from being saved:</h2>

      <ul>
      <% user_mention.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :username %>
    <%= f.text_field :username %>
  </div>

  <div class="field">
    <%= f.label :mentions %>
    <%= f.text_field :mentions %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
